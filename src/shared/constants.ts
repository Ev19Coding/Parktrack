/** Fluff that needs to be trimmed if you're directly dealing with a html fragment string from a component function  */
const ESCAPED_HTML_ELEMENT_FROM_OUTER_HTML = "$<!--$-->";

export { ESCAPED_HTML_ELEMENT_FROM_OUTER_HTML };